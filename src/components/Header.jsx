import React from 'react';
import '../styles/Header.css'; // Assuming there's a CSS file for the Header

const Header = () => {
  return (
    <div className="header-container">
      <h1 className="header-title">Satellite Images of Bulgaria</h1>
    </div>
  );
};

export default Header;
